{
  "name": "tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run format && vite",
    "build": "npm run format && vite build",
    "preview": "vite preview",
    "format": "oxlint --fix && vue-tsc --noEmit --skipLibCheck && prettier --write ."
  },
  "dependencies": {
    "vue": "^3.5.14",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/tsconfig": "^0.7.0",
    "oxlint": "^1.1.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vue-tsc": "^2.2.10"
  },
  "volta": {
    "node": "22.14.0"
  }
}
